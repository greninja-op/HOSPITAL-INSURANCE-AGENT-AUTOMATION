export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8">
      <h1 className="text-3xl font-semibold tracking-tight">AuthPilot</h1>
      <p className="max-w-md text-center text-muted-foreground">
        Autonomous prior-authorization and denial-appeal coordinator. Project
        scaffolding is in place — pages and pipeline arrive in later tasks.
      </p>
    </main>
  );
}
